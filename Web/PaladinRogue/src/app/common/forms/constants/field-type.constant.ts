export enum FieldType {
  NUMBER = 'number',
  PASSWORD = 'password',
  TEXT = 'text',
  SELECT = 'select'
}

/**
 * @deprecated not yet implemented
 */
export enum MaterialSupportedFieldType {
  COLOUR = 'color',
  DATE = 'date',
  DATETIME = 'datetime-local',
  EMAIL = 'email',
  MONTH = 'month',
  SEARCH = 'search',
  TELEPHONE = 'tel',
  TIME = 'time',
  URL = 'url',
  WEEK = 'week'
}
